import React from "react";

const Cart = () => {
  // Placeholder cart page. You can wire this to backend/cart endpoints later.
  return (
    <div className="container mt-4">
      <h2>Cart</h2>
      <p>Your cart is empty (placeholder).</p>
    </div>
  );
};

export default Cart;
